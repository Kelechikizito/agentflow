// It teaches your AI agent how to handle a "pay first, then get access" situation automatically — without any human stepping in.
import * as xrpl from "xrpl";
import { Wallet } from "xrpl";
import * as dotenv from "dotenv";

dotenv.config();
